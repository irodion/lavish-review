{
  "schema": "review-analysis/0.4",
  "title": "Retry backoff becomes exponential; config key renamed",
  "intent_summary": "Replaces the fixed 1s retry delay with capped exponential backoff so a struggling upstream is not hammered at a constant rate, and renames the RETRY_DELAY config key to RETRY_BASE_DELAY to match the new meaning. A drive-by extracts the delay math into a helper and regenerates the lockfile.",
  "widened_into": ["src/client/pool.py"],
  "alignment": {
    "serves_goal": ["t1", "t3"],
    "drive_by": ["t2", "t4"]
  },
  "threads": [
    {
      "id": "t1",
      "title": "Exponential backoff",
      "summary": "The retry loop's delay policy changes from fixed to exponential with a 60s cap — the behavioral heart of the branch. Read this first: everything else is consequence.",
      "paths": ["src/retry.py"],
      "steps": [
        {
          "id": "t1.s1",
          "impact": "behavior-change",
          "summary": "Retry delay changes from a fixed 1s to exponential backoff.",
          "detail": "Before: every retry waited a constant 1s. After: the wait is base * 2**attempt, clamped to 60s. A caller that used to retry 5 times in 5s now spreads those retries across roughly a minute.",
          "confidence": "high",
          "why_now": "Start here — this is the observable behavior change the whole branch exists to make; every other step is downstream of it.",
          "review_prompts": [
            "Compare the old constant 1s delay with the new base * 2**attempt: does the wait now grow with each attempt?",
            "Confirm the 60s cap is applied after the doubling, so no single wait can exceed 60s."
          ],
          "evidence": [
            { "path": "src/retry.py", "hunk": 1 }
          ],
          "attention_notes": [
            { "text": "No test in the diff exercises the new timing — the behavior change is unverified by the suite.", "evidence": [{ "note": "tests/test_retry.py exists but is untouched" }] },
            { "text": "The stated goal asked for 'exponential backoff with jitter'; no thread adds jitter, so the schedule is fully deterministic. Deliberate, or unserved?" }
          ]
        },
        {
          "id": "t1.s2",
          "impact": "unknown-impact",
          "summary": "Whether the 60s cap ever bites depends on the caller's request timeout.",
          "detail": "The cap only changes behavior if a computed wait would exceed the caller's per-request timeout. That timeout lives in src/client/pool.py, which this branch does not touch, so its value — and thus whether the cap is ever reached — is not visible in the diff.",
          "confidence": "low",
          "why_now": "Read right after the policy change: if the pool's per-request timeout is under 60s, the cap never bites and the change is smaller than it looks.",
          "review_prompts": [
            "Read src/client/pool.py's per-request timeout: is it shorter than 60s?",
            "If that timeout is under 60s, can the new cap ever take effect at all?"
          ],
          "evidence": [
            { "note": "widened: src/client/pool.py sets the per-request timeout but was not changed by this branch" }
          ]
        }
      ]
    },
    {
      "id": "t2",
      "title": "Config key rename (drive-by)",
      "summary": "RETRY_DELAY becomes RETRY_BASE_DELAY. Mechanical to write, but it is a public config surface — compatibility is the question, not the code.",
      "paths": ["src/config.py"],
      "steps": [
        {
          "id": "t2.s1",
          "impact": "behavior-change",
          "summary": "RETRY_DELAY is renamed to RETRY_BASE_DELAY; the old key is no longer read.",
          "detail": "The config loader reads only the new name. There is no compatibility shim or deprecation warning in the diff, so an existing RETRY_DELAY in the environment would be ignored rather than honored — but only a grep across every config consumer confirms nothing else still reads the old key.",
          "confidence": "medium",
          "why_now": "Read after the backoff change it supports: this is where the change reaches operators, and the compatibility break lives here, not in the retry math.",
          "review_prompts": [
            "Confirm no deployment target still sets the old RETRY_DELAY key — env, deploy templates, runbooks.",
            "Decide whether the old key should be honored for one release with a deprecation warning."
          ],
          "evidence": [
            { "path": "src/config.py", "hunk": 1 }
          ]
        }
      ]
    },
    {
      "id": "t3",
      "title": "Extract the delay helper",
      "summary": "The delay computation moves out of the retry loop into a compute_delay() helper. Pure extraction — the arithmetic is unchanged; read it to confirm that.",
      "paths": ["src/retry.py"],
      "steps": [
        {
          "id": "t3.s1",
          "impact": "behavior-preserving",
          "summary": "The delay arithmetic is lifted verbatim into a compute_delay() helper.",
          "detail": "The extracted body is the same expression, in the same order (double, then clamp). No new branch, no changed constant; the retry loop now calls the helper. The intent is a readability refactor with no behavior change.",
          "confidence": "high",
          "why_now": "Read alongside the backoff change: this is the same math relocated, so confirming it here lets you trust t1.s1 without re-reading the arithmetic twice.",
          "review_prompts": [
            "Confirm compute_delay() applies the cap after doubling, exactly as the inline version did."
          ],
          "evidence": [
            { "path": "src/retry.py", "hunk": 2 }
          ]
        }
      ]
    },
    {
      "id": "t4",
      "title": "Test and lockfile (drive-by)",
      "summary": "A new timing test and a regenerated lockfile ride along. The test documents the backoff; the lockfile is pure churn.",
      "paths": ["tests/test_retry.py", "poetry.lock"],
      "steps": [
        {
          "id": "t4.s1",
          "impact": "test-change",
          "summary": "A new test pins the delay sequence and the 60s cap.",
          "detail": "It asserts the wait sequence 1, 2, 4, 8, … and that no computed wait exceeds 60s.",
          "confidence": "high",
          "why_now": "Read this once you understand t1.s1: it is the evidence that the new behavior is pinned, and it is the first thing that would fail if the cap regressed.",
          "review_prompts": [
            "Confirm this test fails if the 60s cap is removed, not only if the doubling breaks — otherwise it is not pinning the cap."
          ],
          "relates_to": ["t1.s1"],
          "evidence": [
            { "path": "tests/test_retry.py", "hunk": 1 }
          ]
        },
        {
          "id": "t4.s2",
          "impact": "mechanical-change",
          "summary": "poetry.lock is regenerated with no dependency version changes — hash churn only.",
          "confidence": "high",
          "why_now": "Skim last, or skip: nothing here carries narrative, and no dependency actually moved.",
          "evidence": [
            { "note": "poetry.lock is listed as changed but the classifier omitted its body as lockfile churn" }
          ]
        }
      ]
    }
  ],
  "test_runner": {
    "runner": "pytest",
    "runner_evidence": "pyproject.toml [tool.pytest.ini_options]",
    "command": "pytest"
  },
  "diagrams": []
}
